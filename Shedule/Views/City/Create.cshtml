@model Shedule.Models.Cities
<div>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        
        <div>
            @Html.LabelFor(model => model.Name)
            <p>@Html.EditorFor(model => model.Name)</p>
        </div>
        <p><input type="submit" value="Добавить" /></p>
    }
</div>
