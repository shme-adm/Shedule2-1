@model Cities
@using Shedule.Models
@{
    ViewBag.Title = "Группы";
}

<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
    <h1 class="h2">@ViewBag.Message</h1>
    <div class="btn-toolbar mb-2 mb-md-0">
        @using (Html.BeginForm())
    {
        @Html.ValidationSummary(true)
        <fieldset>            
            <div class="editor-field">
                @Html.EditorFor(model => model.Name)
                @Html.ValidationMessageFor(model => model.Name)
            </div>
            <p><input type="submit" value="Учесть" /></p>
        </fieldset>
}
        <button class="btn btn-sm btn-outline-secondary">
            <span data-feather="plus-circle"></span>
            Новая запись
        </button>
    </div>
</div>
<div class="table-responsive">
    <table class="table table-striped table-dark">
        <thead>
            <tr>
                <th class="" scope="col">№</th>
                <th class="col" scope="col">Наименование</th>
                <th class="" scope="col"></th>
                <th class="" scope="col"></th>
            </tr>
        </thead>
        @foreach (Cities item in ViewBag.Cities)
        {
            int i = 0;
            ++i;
            <tr>
                <th class="" scope="row">@item.Id</th>
                <td class="col">@item.Name</td>
                <td class="text-right"><a><span data-feather="edit"></span></a></td>
                <td class="text-right"><a><span data-feather="trash-2"></span></a></td>
            </tr>
        }
    </table>
</div>
    @*<div class="jumbotron">
            <h2 class="text-center">Вкладка Города</h2>
            <p>@ViewBag.Message</p>
            <table class="table">
                <tr>
                    <td class="text-center"><p>№</p></td>
                    <td class="text-center"><p>Наименование</p></td>
                    <td><p></p></td>
                    <td><p></p></td>
                </tr>
                @foreach (Shedule.Models.Cities c in Model)
                {
                    int i = 0;
                    <tr>
                        <td><p>@i++</p></td>
                        <td><p>@c.Name</p></td>
                        <td><a><span data-feather="edit"></span></a></td>
                        <td><a><span data-feather="trash-2"></span></a></td>
                    </tr>
                }
            </table>
        </div>*@
